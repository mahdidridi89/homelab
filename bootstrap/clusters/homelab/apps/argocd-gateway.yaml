apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: argocd-gateway
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/<TON-UTILISATEUR>/homelab.git
    targetRevision: HEAD
    path: infrastructure/networking/argocd-gateway

  destination:
    server: https://kubernetes.default.svc
    namespace: argocd

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
